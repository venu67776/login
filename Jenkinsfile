@Library('todoapp') _

todoapp (
        COMPONENT             : 'login',
        PROJECT_NAME          : "todo",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)