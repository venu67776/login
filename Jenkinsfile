@Library('todo') _

todo (
        COMPONENT             : 'login',
        PROJECT_NAME          : "todoapp",
        SLAVE_LABEL           : "GOLANG",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)